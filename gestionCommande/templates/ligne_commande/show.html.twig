{% extends 'base.html.twig' %}

{% block title %}LigneCommande{% endblock %}

{% block body %}

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ligne_commande.id }}</td>
            </tr>
            <tr>
                <th>IdCom</th>
                <td>{{ ligne_commande.idCom }}</td>
            </tr>
            <tr>
                <th>IdProduit</th>
                <td>{{ ligne_commande.idProduit }}</td>
            </tr>
            <tr>
                <th>QteCom</th>
                <td>{{ ligne_commande.qteCom }}</td>
            </tr>
        </tbody>
    </table>

    <button type="button" class="btn btn-info"><a href="{{ path('ligne_commande_index') }}" class="text-white" style="text-decoration:none;" >Retour</a></button>

    <button type="button" class="btn btn-warning"><a href="{{ path('ligne_commande_edit', {'id': ligne_commande.id}) }}" class="text-white" style="text-decoration:none;" >Modifier</a></button>

    {{ include('ligne_commande/_delete_form.html.twig') }}
{% endblock %}
